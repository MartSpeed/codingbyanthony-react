import React from "react"

const SearchBox = () => {
    return(
        <React.Fragment>
            <input type="search" placeholder="search robots"/>
        </React.Fragment>
    );
}

export default SearchBox;